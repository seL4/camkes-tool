# The Controller python module.

# This module contains graph_controller, which is the main window that the user sees. GraphController deals with the getting a file specified by the user, high level event handling (like File, View menus), and creating an AST from the model (ASTModel) to give to View(GraphWidget)
